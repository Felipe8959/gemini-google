from sumy.parsers.plaintext import PlaintextParser
from sumy.nlp.tokenizers import Tokenizer
from sumy.summarizers.lsa import LsaSummarizer

# Texto para sumarizar
text = """
O Python é uma linguagem de programação de alto nível, interpretada, de script e orientada a objetos com semântica dinâmica.
Sua tipagem dinâmica e a fácil legibilidade dos seus códigos a tornam ideal para prototipagem rápida e desenvolvimento de aplicativos.
Além disso, Python oferece módulos e pacotes, o que incentiva a modularidade e a reutilização de códigos.
"""

# Parser de texto
parser = PlaintextParser.from_string(text, Tokenizer("portuguese"))
# Escolha do sumarizador (neste caso, LSA)
summarizer = LsaSummarizer()
# Número de frases no resumo
summary = summarizer(parser.document, 2)

# Exibindo o resumo
for sentence in summary:
    print(sentence)
