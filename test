def identificar_tipo_documento(doc):
    doc = remover_formatacao(doc)
    if len(doc) == 11:
        return "CPF"
    elif len(doc) == 14:
        return "CNPJ"
    else:
        return "Desconhecido"

# Adicionando a coluna ao DataFrame
df['tipo_documento'] = df['documentos'].apply(identificar_tipo_documento)
